import king from "./king";
import crow from "./crow";
import zero from "./zero";

const matrix = {
  itemId: 2,
  itemName: 'Matrix',
  itemLogo: 'none',
  itemContents: [
    king,
    crow,
    zero,
  ]
}

export default matrix;