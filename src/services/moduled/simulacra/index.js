import king from "./king";
import crow from "./crow";
import zero from "./zero";

const simulacra = {
  itemId: 1,
  itemName: 'Simulacra',
  itemLogo: 'none',
  itemContents: [
    king,
    crow,
    zero,
  ]
}

export default simulacra;